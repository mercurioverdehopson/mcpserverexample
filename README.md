Add the following code to json config to add mcp:

"example": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/mercurioverdehopson/mcpserverexample.git",
        "mcp-server"
      ]
    }